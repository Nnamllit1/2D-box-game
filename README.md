# 2D box game

A simple 2D box game (it at least wants to be a game) written in C++.

## Requirements

- CMake
- SDL2

## Build

```bash
cmake -B build -S .
cmake --build build
```

## Run

```bash
build/game
```
